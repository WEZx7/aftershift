# Deploy AFTERSHIFT v1.1

This ZIP is a cleaned full repository snapshot.

In Codespaces, replace the project files with the contents of this folder, then run:

```bash
git add .
git commit -m "Release AFTERSHIFT v1.1 immersion update"
git push
```

Then hard-refresh GitHub Pages. Existing player progress remains compatible because the campaign save keys were not changed.
